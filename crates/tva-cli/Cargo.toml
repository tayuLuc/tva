[package]
name = "tva-cli"
version = "0.1.0"
edition = "2021"
description = "CLI for Temporal Video Analyzer"
license = "MIT"

[dependencies]
tva-core = { path = "../tva-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
csv = "1"
